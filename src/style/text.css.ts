import { style } from '@vanilla-extract/css';

// text align
export const text_left = style({
  textAlign: 'left',
});
export const text_center = style({
  textAlign: 'center',
});
export const text_right = style({
  textAlign: 'right',
});
